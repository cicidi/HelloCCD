import java.util.Random;

public class test {

	public static void main(String[] args) {
		Random rand1 = new Random();

		int m = rand1.nextInt(1000000);
		int n = rand1.nextInt(1000000);
		int temp;

		if (m > n) {
			temp = m;
			m = n;
			n = temp;
		}
		int temp2 = n / m;
		System.out.println("M" + m);
		System.out.println("N" + n);
		System.out.println("Temp2" + temp2);
		System.out.println("n/m" + n / m);
		test.getMaxInt(m, n);
		test.getMaxInt(80, 100);
		test.getMaxInt(88, 90);
		test.getMaxInt(88, 89);
		test.getMaxInt(85, 87);
		test.getMaxInt(87, 92);
	}

	public static void getMaxInt(int min, int max) {
		int result = 0;

		if (max - min > 100) {
			result = (max / 100) * 100 - 1;
		} else if (max - min > 10) {
			result = (max / 10) * 10 - 1;
		} else if (max - min < 10 && max % 10 == 0) {
			result = max - 1;
		} else if ((max / 10 - min / 10) != 0)
			result = max / 10 * 10 - 1;
		else
			result = max;

		// System.out.println(result);
	}

}